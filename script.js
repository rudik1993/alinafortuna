document.addEventListener('DOMContentLoaded', function () {
    setTimeout(() => {
        document.querySelector('.animation').style.display = 'none';
    }, 3000);  // Wait 3 seconds for the animation to finish
});
